# frozen_string_literal: true

namespace :ranker do
  desc 'Load data from a given file path into the current environment. See spec/test_data/ for examples of format'
  task load_data: :environment do
  end
end
